/**
 * @deprecated Use `npm run migrate:banks-from-leads` (tsx scripts/migrate-banks-from-leads.ts).
 * Bank codes are generated via the tenant BANK code-generation template.
 */
console.error('This script is deprecated. Run: npm run migrate:banks-from-leads')
process.exit(1)
